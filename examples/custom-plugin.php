<?php
/**
 * Synthetic example plugin pattern.
 *
 * This is intentionally minimal and contains no production credentials.
 */

defined('ABSPATH') || exit;

add_action('init', function (): void {
    // Register application behavior here.
});
